# filepath: /home/tom/Depotgit/42-matcha/frontend/nginx.conf
server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API proxy example if needed via Kong (frontend should call Kong on 8443)
    # location /users {
    #     proxy_pass http://kong_gateway:8000/users;
    # }
}
